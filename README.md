# Deployment
```In order to deploy this environment Please ensure You have ansible install to decrypt the password```
# Install Packages
```brew install ansible```
```brew install terraform```
```brew cask install gcloud and then follow gcloud tutorial to setup gcloud```
# Decrypt the file
```ansible-vault decrypt (the name of the files) then the password```
# Setup Env
# Check for syntax error
```terraform plan```
# deploy
```terraform apply```
# setup terminal with gcloud kubernetes"

